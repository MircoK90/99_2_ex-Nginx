chmod +x tests/run_tests.sh


mkdir -p deployments/nginx/certs
openssl req -x509 -nodes -days 365 \
  -newkey rsa:2048 \
  -keyout deployments/nginx/certs/nginx.key \
  -out deployments/nginx/certs/nginx.crt

  htpasswd -c deployments/nginx/.htpasswd admin


# Renewing secound Version mirco stable.  openssl part of LINUX

  openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout deployments/nginx/certs/nginx.key \
  -out deployments/nginx/certs/nginx.crt \
  -subj "/CN=localhost" \
  -addext "subjectAltName=DNS:localhost,IP:127.0.0.1"

# admin pw create
htpasswd -nb admin admin > deployments/nginx/.htpasswd



# Manuell api test
curl -X POST "https://localhost/predict" \
  -H "Content-Type: application/json" \
  -d '{"sentence": "What can i now do?"}' \
  --user admin:admin \
  --cacert ./deployments/nginx/certs/nginx.crt


