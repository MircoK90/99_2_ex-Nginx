# nginx/nginx.conf

worker_processes auto;          # cpu core control

events {
    worker_connections 1024;    # max number of connections per worker/core
}

http{
    # Rate limiting configuration
    limit_req_zone $binary_remote_addr zone=apr_limit:10m rate=10r/s;   # ip client, 10mb of memory, 10 requests per second

    # Upstream
    upstream api-1 {
        server api-v1:8080;
        server api-v2:8080;
        server api-v3:8080;
    }

    upstream api-2 {
        server api-v1:8080;
    }

    server {
        listen 80;
        return 301 https://$host$request_uri;
    }

    server{
        listen 443 ssl;
        ssl_certificate /etc/nginx/certs/nginx.crt;
        ssl_certificate_key /etc/nginx/certs/nginx.key;

        # Basic Authentication
        auth_basic "Restricted";
        auth_basic_user_file /etc/nginx/.htpasswd;

        # Rate limiting
        limit_req zone=api_limit burst=20 nodelay;

        location /predict {
            # v1 / v2 testing
            if ($http_x_experiment_group = "debug") {
                proxy_pass http://api_v2;
                break;
            }
            proxy_pass http://api_1;
        }        
    }
}